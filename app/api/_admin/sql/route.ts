// This route lives in a `_`-prefixed folder, which Next.js treats as private
// and does not expose. The active admin bridge is at /api/admin-bridge/sql.
// This file is a placeholder so the filename doesn't cause build confusion;
// it can be deleted.
export const runtime = "nodejs";
